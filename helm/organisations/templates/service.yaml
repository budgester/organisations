apiVersion: v1
kind: Service
metadata:
  name: {{ include "organisations.fullname" . }}
  labels:
    {{- include "organisations.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "organisations.selectorLabels" . | nindent 4 }}
